name: Workflow Demo

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      

      - name: Print message
        run: echo "Workflow started"

    #   - name: Can we deploy today?
    #     uses: timelfrink/shouldideploy-action@v1
    #     with:
    #       timezone: 'IST'
        
      - name: I will do it myself
        run: | 
            RESPONSE=$(curl -s https://shouldideploy.today/api?tz=Asia%2FKolkata&lang=en)
            echo "Response: $RESPONSE.message"

            
      - name: Deploy
        run: echo "Deploying to production..."