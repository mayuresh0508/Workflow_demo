name: Workflow Demo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print message
        run: echo "This is a demo of a workflow."

      - name: Can we deploy today?
        uses: armanbilge/should-i-deploy-today@v1

      - name: Deploy
        run: echo "Deploying to production..."